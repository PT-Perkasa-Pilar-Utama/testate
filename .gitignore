# dependencies
node_modules

# build output
dist
apps/web/dist
apps/api/dist

# runtime data (metadata.db, blobs, logs, uploads, run/)
/data/
# a dev server started from apps/api leaves its instance here
apps/api/data/
*.db
*.db-shm
*.db-wal

# dotenv
.env
.env.*
!deploy/.env.example
!apps/api/.env.example

# caches
.cache
*.tsbuildinfo

# editors and OS
.idea
.vscode
.DS_Store

# smoke test output
.smoke
.e2e

# docker-slim writes this next to the invocation
slim.report.json

# compiled binaries (scripts/binary/build-binaries.ts)
.bin-build
